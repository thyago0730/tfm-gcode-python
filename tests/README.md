Testes automatizados

Esta pasta agrupa os scripts de teste do projeto.
Execute-os a partir da raiz do repositório, por exemplo:

```
python tests/teste_rotacao_unica.py
```

Os testes ajustam o `sys.path` relativo à raiz para importar `src/app/TFM_GCODE.py` corretamente.
